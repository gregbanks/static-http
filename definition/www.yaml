$parameters:
  node_pool:
    type: string
  num_instances:
    type: number

type: v1/service

build:
  type: docker_build
  docker_file:
    path: context
  build_context:
    path: context
  environment: {}
  options: {}
ports:
  "8888":
    protocol: TCP

node_pool: ${node_pool}
num_instances: ${num_instances}
